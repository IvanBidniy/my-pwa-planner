# CHANGELOG

## v1.0.0 (2026-01-28)

### Added

-   Створено базові файли `style.css` та `main.js`.
-   Реалізовано функцію `updateClock` для живого оновлення часу щосекунди.
-   Додано fetch-запит до Open-Meteo для отримання поточної погоди у Києві.
-   Створено тестовий масив даних для календаря та функцію `renderCalendar` для його відображення у вигляді списку.
-   Оновлено `index.html` для відображення годинника, погоди та календаря.
-   Додано базові стилі для `clock`, `weather`, та `calendar` в `style.css`.
-   Створено цей `CHANGELOG.md` для відстеження змін у проекті.
